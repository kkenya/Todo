<div class="page-header">
  <h2>タスクの編集</h2>
</div>

<p><%= link_to "タスクの一覧に戻る", [current_user, :tasks] %></p>

<%= form_for [current_user,@task], role: 'form' do |form| %>
  <%= render "form", form: form %>
  <%= form.submit '更新', class: 'btn btn-default', data: { disable_with: '更新中' } %>
<% end %>